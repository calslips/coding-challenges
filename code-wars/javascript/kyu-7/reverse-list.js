/*
Write reverseList function that simply reverses lists.

Parameters: an array
Returns: an array
  elements in reverse order from input
Examples:
  reverseList([]), []
  reverseList([1,2,3]), [3,2,1]
Pseudocode:
  return result of reverse method called on input array
*/

function reverseList(arr) {
  return arr.reverse();
}